interface:
  display_name: "MetalDocs Frontend"
  short_description: "Implement React features with Zustand stores, CSS Modules, domain-split API, and focused components. The permanent standard for all frontend work."
  default_prompt: "Use $metaldocs-frontend when implementing any React feature, page, or component."
policy:
  allow_implicit_invocation: true
