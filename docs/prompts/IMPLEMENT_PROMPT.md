# Official Prompt - Implementation

Implement only what is requested, with the smallest safe diff.
Follow `AGENTS.md` and architecture guardrails strictly.
Requirements:
- Keep module boundaries intact.
- Update OpenAPI when API changes.
- Add tests required by standards.
- Update docs/runbook when infra or behavior changes.
Never add IA/LLM runtime features in v1.
